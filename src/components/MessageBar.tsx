'use client';

import 'bootstrap/dist/css/bootstrap.min.css';

const MessageBar = () => (
  <div className="bg-dark text-white text-center py-2 message-bar">
    Free shipping on US orders over $50
  </div>
);

export default MessageBar;
